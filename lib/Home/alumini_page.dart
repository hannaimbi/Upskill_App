import 'package:flutter/material.dart';

class AlumniPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Alumni Connect")),
      body: Center(child: Text("Alumni Page Content Here")),
    );
  }
}
